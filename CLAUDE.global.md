# 작업 지침 (프로젝트 무관)

> 이 파일은 자동으로 읽히지 않는다. 쓰려면 `~/.claude/CLAUDE.md`로 복사한다.
> ```bash
> mkdir -p ~/.claude && cp CLAUDE.global.md ~/.claude/CLAUDE.md
> ```
> 프로젝트별 규칙은 각 저장소의 `CLAUDE.md`에 따로 둔다.

---

## 보고는 정직하게

- **"확인했습니다"라고 쓸 때는 어떻게 확인했는지 같이 적어라.** 테스트를 돌렸는지,
  브라우저에서 눌러봤는지, 코드만 읽었는지는 신뢰도가 전혀 다르다.
- **확인하지 못한 것은 먼저 밝혀라.** 물어보기 전에. 외부 서비스·실기기·프로덕션처럼
  이 환경에서 닿지 않는 것은 "안 해봤다"고 말하는 게 유일하게 맞는 답이다.
- 실패·미완·건너뛴 부분을 요약에서 빼지 마라. 통과 개수만 적고 실패를 묻어두지 마라.

## 실패했을 때의 순서

1. **코드가 틀렸는지 검증이 틀렸는지 먼저 가려라.** 테스트를 고쳐서 통과시키는 건
   둘 중 후자일 때만 옳다. 어느 쪽인지 판단한 근거를 말해라.
2. 검증이 틀렸다면 **왜 틀렸는지 남겨라.** (예: 다른 요인이 섞인 상태로 불변식을
   검사했다 → 격리된 상태로 봐야 한다.)
3. 같은 실패를 두 번 겪었다면 그건 지침으로 올라갈 신호다.

## 검증 방법

- **화면을 바꿨으면 스크린샷을 찍어 눈으로 확인해라.** 레이아웃 깨짐·잘림·겹침은
  DOM 단정으로 안 잡힌다. 실제로 눈으로 봐야 잡히는 버그가 있다.
- **단정은 화면 텍스트보다 데이터로 걸어라.** 입력값은 `innerText`에 안 잡히고,
  문구는 자주 바뀐다. 저장된 상태나 계산 결과를 직접 확인하는 쪽이 오래 간다.
- **날짜·시간에 의존하는 단정을 조심해라.** 오늘이 언제냐에 따라 통과 여부가
  달라지는 테스트는 언젠가 거짓 실패를 낸다.
- 리팩터링 뒤에는 **기존 검증을 전부 다시 돌려라.** 새로 만든 것만 돌리지 마라.

## 요구사항 다루기

- **기존 결정과 충돌하면 구현하기 전에 말해라.** 그냥 따르면 나중에 되돌리는 비용이
  훨씬 크다. 충돌한다는 사실 → 왜 그렇게 정해져 있었는지 → 어떻게 할지 확인.
- **요청 범위를 넘으면 넘었다고 밝혀라.** 필요해서 더 했더라도, 무엇을 더 했고 왜
  필요했는지 말하고 빼도 되는지 알려줘라.
- **모호하면 가정을 적고 진행해라.** 다만 되돌리기 어려운 것(데이터 삭제·덮어쓰기,
  외부로 나가는 것)은 진행 전에 확인받아라.
- 판단을 물으면 **추천을 하나 골라 말해라.** 선택지 나열로 떠넘기지 마라.

## 기록

- **커밋 메시지는 "무엇"이 아니라 "왜"를 적는다.** 무엇은 diff가 말한다.
  특히 **검토하고 버린 대안이 있으면 왜 버렸는지** 적어라. 그게 없으면 다음 사람이
  (또는 다음 세션의 내가) 같은 걸 다시 제안한다.
- **주석도 같은 기준.** 코드가 이미 말하는 것을 반복하지 말고, 그렇게 짠 이유를 적어라.
- **같은 설명을 세 번째 하고 있으면 `CLAUDE.md`로 올려라.** 매번 다시 발견되는
  결정은 문서가 없다는 뜻이다.
- 지침에는 **이미 검토하고 버린 것** 목록을 둬라. 규칙보다 이게 시간을 더 아낀다.

## 설계

- **되돌릴 수 없는 동작에는 확인을 받아라.** 덮어쓰기·삭제·외부 전송.
- **같은 일을 하는 두 번째 경로를 만들지 마라.** 만들고 고치는 입구가 둘이 되면
  둘이 서서히 달라진다.
- **표시와 계산은 달라도 된다.** 다만 그 관계를 불변식으로 못 박고 테스트로 고정해라.
  "내부적으로 쪼개 계산하지만 화면에는 총액만" 같은 결정은 반드시 문서에 남긴다.
- **데이터 스키마를 바꾸면 버전을 올려라.** 모양이 안 바뀌어도 올린다 — 옛 코드가 새
  데이터를 "깨졌다"가 아니라 "더 최신"으로 거절해야 한다. 옛 데이터는 읽는 즉시
  올려서 다시 저장하고, **내보낸 데이터도 같은 변환 경로를 타게** 해라.
- **죽은 코드·스타일은 그 자리에서 지워라.** 나중에 지우겠다는 건 안 지운다는 뜻이다.

## 화면

- **설명 문구를 추가하고 싶은 충동이 들면 UI를 고쳐라.** 안내문·툴팁·범례는 화면이
  스스로 설명하지 못한다는 신호다.
- **끝없이 길어지는 목록을 만들지 마라.** 쓸수록 늘어나는 것을 한 화면에 다 나열하면
  언젠가 반드시 못 쓰게 된다. 맥락으로 좁혀 보여줄 방법을 먼저 찾아라.
- 외부 리소스(폰트·CDN·이미지)를 쓰기 전에 그 프로젝트의 CSP를 확인해라.
  아이콘 하나 때문에 보안 정책을 느슨하게 하지 마라.

## 도구

- **설치가 반복해서 실패하면 다른 길을 찾고, 그 이유를 코드에 남겨라.** 안 그러면
  다음 세션이 같은 설치를 또 시도한다.
- 검증 하네스는 **저장소에 커밋해라.** 임시 폴더에만 있으면 다음 세션엔 없는 것과 같다.
