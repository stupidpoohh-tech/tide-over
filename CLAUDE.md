# 잔고 캘린더 (tide-over) — 작업 지침

가용돈이 빠듯한 사람이, 통장 잔고 하나와 앞으로의 예정 수입·지출을 넣고
"다음 입금까지 이 돈으로"를 확인하는 웹앱.

이 문서는 **다시 발견되면 안 되는 결정들**을 적어 둔 것이다. 여기 적힌 것과
충돌하는 요청을 받으면 그냥 따르지 말고, **충돌한다는 사실을 먼저 말하고**
어떻게 할지 확인받아라. 여러 번 뒤집힌 끝에 지금 모양으로 온 것들이다.

---

## 1. 핵심 모델 — 이 앱의 정체성

**입력의 원자는 지출이 아니라 잔고다. 미래는 달력에, 과거는 잔고에.**

- 과거 지출은 **입력하지 않는다.** 통장 잔고를 옮겨 적는 순간, 그 사이의 모든
  변동 지출이 정산된 것으로 본다.
- 달력에는 **오늘 이후의 예정 입금·출금만** 존재한다.
- 경계는 항상 오늘이다. 오늘 이전 = 잔고가 말함, 오늘 이후 = 달력이 말함.
- 이 앱은 **예측하지 않는다.** 표시하는 숫자는 "예상 잔고"가 아니라 "이 날까지
  쓸 수 있는 한도"다. 변동 지출을 추정하지 않는 것은 **누락이 아니라 정의다.**

"평균 지출로 예측", "지난달 대비 분석", "카테고리별 통계" 같은 요청이 오면
이 정의와 충돌한다. 구현 전에 짚어라.

## 2. 계산 불변식 — 깨면 안 되는 것

전부 `src/lib/calc.ts`에 있다. 계산 로직은 이 파일 밖으로 새지 않는다.

```
limit(d)  = 현재 잔고 + (오늘 이후 ~ d일까지의 예정 입금 − 예정 출금)
머리 숫자 = limit(끝점)
정산 diff = 새로 적은 잔고 − (직전 잔고 + 그 사이 지나간 예정 입금 − 예정 출금)
끝점      = 다음 예정 입금 전날. 예정 입금이 없으면 30일 뒤 (horizonOf)
```

**불변식 1 — 구간은 `(after, through]` 하나로 통일한다.**
시작은 열려 있고 끝은 닫혀 있다. `limit`이 앞으로 반영하는 금액과 `정산 diff`가
뒤로 반영하는 금액이 정확히 맞물려야 하고, 같은 항목이 두 번 세지거나 빠지면
안 된다. 오늘 날짜에 잡힌 예정은 **이미 잔고에 반영된 것으로 본다.**

**불변식 2 — 급여일은 별도 필드가 아니다.**
급여도 그냥 예정 입금이다. 끝점은 "다음 예정 입금 전날"로 일반화돼 있어서,
주급(7일마다 입금)도 같은 규칙으로 돌아간다. `payday` 같은 필드를 다시 만들지 마라.

**불변식 3 — 기간 예산(생활비)은 표시와 계산이 다르다.**
| | 규칙 |
|---|---|
| 표시 | 총액 고정. 달력에 막대 하나 + "생활비 10만". **하루 몫(9,090원 같은)을 어디에도 보여주지 않는다.** 목록·정산 원장도 `summarize()`로 한 줄 요약 |
| 한도 | 기간에 들어서는 순간 **남은 몫 전체를 예약.** 기간 안에서는 상수이고, 마지막 날이 지나도 같은 값 |
| 정산 | 내부적으로 **하루 몫 페이스**가 기준 (나머지는 마지막 날에 몰아줌) |

시작일 전액 차감(정산 후 한도 부풀림)과 마지막 날 일괄 차감(기간 지출이 전부
돌발로 잡힘)은 이미 검토하고 버렸다. 다시 제안하지 마라.

기간 **입금**은 `horizonOf`의 주기 기준에서 뺀다 — 매일 조금씩 들어오는 흐름을
"다음 입금일"로 삼으면 끝점이 늘 내일이 된다.

**불변식 4 — 한 번짜리 예정은 내일 날짜부터만 만들 수 있다.**
오늘까지는 잔고가 이미 말해주고 있어서, 오늘 날짜에 넣으면 이중으로 세어진다.
매달·N일마다·기간은 시작이 과거여도 된다(앞으로의 발생분만 잡힌다).

## 3. 화면 원칙

화면은 **달력**과 **설정** 둘뿐이다. 나머지는 전부 팝업.

- **설명 문구를 넣지 않는다.** 화면이 스스로 설명해야 한다. 안내문·툴팁·범례
  텍스트를 추가하려는 충동이 들면, 대신 UI를 고쳐라. (여러 번 넣었다 지웠다.)
- **끝없이 길어지는 목록을 만들지 않는다.** 항목 전체를 나열하는 화면은 없다.
  달력에서 날짜를 눌러 그 날 것만 본다. 설정에 목록을 다시 넣지 마라.
- 달력 아래 카드는 맥락에 따라 바뀐다: 아무것도 안 고르면 **끝점까지 남은 예정**,
  날짜를 고르면 **그 날의 내역 리스트**.
- 만들고 고치는 경로는 `EntryDialog` 하나다. 두 번째 폼을 만들지 마라.
- 달력 빈칸은 앞뒤 달 날짜로 채우고 흐리게(opacity) 보여준다.

## 4. 저장

서버도, 계정도, 자동 동기화도 없다. **이건 범위 밖이고 앞으로도 그렇다.**

- `localStorage` + 스키마 버전 키(`tideover.schema` / `tideover.state`).
- 스키마를 바꾸면 `src/lib/storage.ts`의 `MIGRATIONS`에 `v -> v+1` 변환을 넣고
  `SCHEMA_VERSION`을 올린다. 모양이 안 바뀌어도 **버전은 올린다** — 옛 코드가
  새 데이터를 "깨졌다"가 아니라 "더 최신 버전"으로 거절해야 하기 때문이다.
- 옛 데이터는 읽는 즉시 올려서 다시 써둔다. 앞으로의 버전 데이터는 **읽지 않고
  경고만** 띄운다(덮어쓰지 않는다).
- **백업 링크도 같은 마이그레이션 경로를 탄다.** v1 때 만든 링크가 지금도 열려야
  한다 (`decodeBackup` → `migrateToCurrent`).
- 백업 이력은 localStorage와 **쿠키 양쪽에** 남긴다. 이력이 localStorage에만
  있으면 데이터와 같이 날아가서, 지워진 걸 알아챌 방법이 없어진다.
- 상태를 덮어쓰기 전에는 **반드시 확인을 받는다.**

## 5. 배포 제약

- CSP가 `default-src 'self'`다. **외부 리소스(폰트·CDN·이미지)를 쓸 수 없다.**
  아이콘이 필요하면 인라인 SVG로 그려라.
- `Referrer-Policy: no-referrer` — 백업 링크에 상태 전체가 들어 있어서 referrer로
  새면 안 된다.
- `vite.config.ts`의 `base: './'` 유지. 어느 하위 경로에 올려도 동작해야 한다.
- 정책은 `public/_headers`(Cloudflare)와 `vercel.json`(Vercel)에 **양쪽 다** 있다.
  한쪽만 고치지 마라.

## 6. 작업 방식

- **끝내기 전에 반드시:** `npm test` → `npm run build` → `npm run smoke`.
- 계산을 건드렸으면 **불변식을 고정하는 테스트를 같이 써라.** 특히 §2의 네 가지.
  `src/lib/calc.test.ts`의 "정산 후 다시 계산해도 한도가 어긋나지 않는다" 참고.
- UI를 건드렸으면 **실제 브라우저에서 확인해라.** `npm run smoke`가
  `tools/browser.mjs`(사전 설치 Chromium에 CDP로 직접 접속)로 돌아간다.
  playwright는 이 환경의 프록시에서 설치가 끊기니 다시 시도하지 마라.
  `tools/serve.mjs`가 배포와 같은 CSP를 얹어 서빙하므로 CSP 위반도 여기서 잡힌다.
  화면을 바꿨으면 `shot()`으로 스크린샷을 찍어 **눈으로도 확인해라.**
- 스모크가 실패하면 **앱이 틀렸는지 테스트가 틀렸는지 먼저 가려라.** 예: "기간 안의
  한도는 상수"는 다른 예정이 섞이면 당연히 깨진다 — 격리된 상태로 봐야 한다.
- **"확인했습니다"라고 쓸 때는 어떻게 확인했는지 같이 적어라.** 확인 못 한 게
  있으면 그것도 밝혀라. (예: 외부 도메인은 이 환경에서 막혀 로딩까지는 검증 불가.)
- 커밋 메시지는 **무엇을 했는지가 아니라 왜 그렇게 했는지**를 적는다. 특히 버린
  대안이 있으면 왜 버렸는지.
- 주석도 같은 기준. "무엇"은 코드가 말하므로, 주석은 **왜**를 적는다.

## 7. 범위 밖

서버, 계정, 자동 동기화, 변동 지출 입력, 지출 예측, 카테고리 통계.

밈·바이럴 요소(캐릭터, 랜덤 문구, 놀리는 카피)는 **검토했고 폐기했다.**

## 8. 형제 서비스

[loan-early](https://loan-early.vercel.app/) — 대출 조기상환 계산기. 설정의
"서비스 더보기"에서 **링크로만** 이어져 있다. 데이터를 주고받지 않는다:
이쪽은 "다음 입금까지 버티기"(일 단위 추적), 저쪽은 "몇 년 뒤 이자
아끼기"(개월 단위 시뮬레이션)라 시간 축이 다르다. 탭으로 합치면 앱의 정체가
흐려진다.

---

# 작업 습관 (프로젝트 무관)

아래는 이 앱과 상관없이 늘 지키는 것들이다. 다른 저장소에 그대로 복사해도 된다.

### 보고는 정직하게

- **"확인했습니다"라고 쓸 때는 어떻게 확인했는지 같이 적어라.** 테스트를 돌렸는지,
  브라우저에서 눌러봤는지, 코드만 읽었는지는 신뢰도가 전혀 다르다.
- **확인하지 못한 것은 먼저 밝혀라.** 물어보기 전에. 외부 서비스·실기기·프로덕션처럼
  이 환경에서 닿지 않는 것은 "안 해봤다"고 말하는 게 유일하게 맞는 답이다.
- 실패·미완·건너뛴 부분을 요약에서 빼지 마라. 통과 개수만 적고 실패를 묻어두지 마라.

### 실패했을 때의 순서

1. **코드가 틀렸는지 검증이 틀렸는지 먼저 가려라.** 테스트를 고쳐서 통과시키는 건
   둘 중 후자일 때만 옳다. 어느 쪽인지 판단한 근거를 말해라.
2. 검증이 틀렸다면 **왜 틀렸는지 남겨라.** (예: 다른 요인이 섞인 상태로 불변식을
   검사했다 → 격리된 상태로 봐야 한다.)
3. 같은 실패를 두 번 겪었다면 그건 지침으로 올라갈 신호다.

### 검증 방법

- **화면을 바꿨으면 스크린샷을 찍어 눈으로 확인해라.** 레이아웃 깨짐·잘림·겹침은
  DOM 단정으로 안 잡힌다. 실제로 눈으로 봐야 잡히는 버그가 있다.
- **단정은 화면 텍스트보다 데이터로 걸어라.** 입력값은 `innerText`에 안 잡히고,
  문구는 자주 바뀐다. 저장된 상태나 계산 결과를 직접 확인하는 쪽이 오래 간다.
- **날짜·시간에 의존하는 단정을 조심해라.** 오늘이 언제냐에 따라 통과 여부가
  달라지는 테스트는 언젠가 거짓 실패를 낸다.
- 리팩터링 뒤에는 **기존 검증을 전부 다시 돌려라.** 새로 만든 것만 돌리지 마라.

### 요구사항 다루기

- **기존 결정과 충돌하면 구현하기 전에 말해라.** 그냥 따르면 나중에 되돌리는 비용이
  훨씬 크다. 충돌한다는 사실 → 왜 그렇게 정해져 있었는지 → 어떻게 할지 확인.
- **요청 범위를 넘으면 넘었다고 밝혀라.** 필요해서 더 했더라도, 무엇을 더 했고 왜
  필요했는지 말하고 빼도 되는지 알려줘라.
- **모호하면 가정을 적고 진행해라.** 다만 되돌리기 어려운 것(데이터 삭제·덮어쓰기,
  외부로 나가는 것)은 진행 전에 확인받아라.
- 판단을 물으면 **추천을 하나 골라 말해라.** 선택지 나열로 떠넘기지 마라.

### 기록

- **커밋 메시지는 "무엇"이 아니라 "왜"를 적는다.** 무엇은 diff가 말한다.
  특히 **검토하고 버린 대안이 있으면 왜 버렸는지** 적어라. 그게 없으면 다음 사람이
  (또는 다음 세션의 내가) 같은 걸 다시 제안한다.
- **주석도 같은 기준.** 코드가 이미 말하는 것을 반복하지 말고, 그렇게 짠 이유를 적어라.
- **같은 설명을 세 번째 하고 있으면 `CLAUDE.md`로 올려라.** 매번 다시 발견되는
  결정은 문서가 없다는 뜻이다.
- 지침에는 **이미 검토하고 버린 것** 목록을 둬라. 규칙보다 이게 시간을 더 아낀다.

### 설계

- **되돌릴 수 없는 동작에는 확인을 받아라.** 덮어쓰기·삭제·외부 전송.
- **같은 일을 하는 두 번째 경로를 만들지 마라.** 만들고 고치는 입구가 둘이 되면
  둘이 서서히 달라진다.
- **표시와 계산은 달라도 된다.** 다만 그 관계를 불변식으로 못 박고 테스트로 고정해라.
  "내부적으로 쪼개 계산하지만 화면에는 총액만" 같은 결정은 반드시 문서에 남긴다.
- **데이터 스키마를 바꾸면 버전을 올려라.** 모양이 안 바뀌어도 올린다 — 옛 코드가 새
  데이터를 "깨졌다"가 아니라 "더 최신"으로 거절해야 한다. 옛 데이터는 읽는 즉시
  올려서 다시 저장하고, **내보낸 데이터도 같은 변환 경로를 타게** 해라.
- **죽은 코드·스타일은 그 자리에서 지워라.** 나중에 지우겠다는 건 안 지운다는 뜻이다.

### 화면

- **설명 문구를 추가하고 싶은 충동이 들면 UI를 고쳐라.** 안내문·툴팁·범례는 화면이
  스스로 설명하지 못한다는 신호다.
- **끝없이 길어지는 목록을 만들지 마라.** 쓸수록 늘어나는 것을 한 화면에 다 나열하면
  언젠가 반드시 못 쓰게 된다. 맥락으로 좁혀 보여줄 방법을 먼저 찾아라.
- 외부 리소스(폰트·CDN·이미지)를 쓰기 전에 그 프로젝트의 CSP를 확인해라.
  아이콘 하나 때문에 보안 정책을 느슨하게 하지 마라.

### 도구

- **설치가 반복해서 실패하면 다른 길을 찾고, 그 이유를 코드에 남겨라.** 안 그러면
  다음 세션이 같은 설치를 또 시도한다.
- 검증 하네스는 **저장소에 커밋해라.** 임시 폴더에만 있으면 다음 세션엔 없는 것과 같다.
